# CodeMgr v1.8 — 未完成项清零（IPv6 / 刷新间隔 / 虚拟列表 / a11y）Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把已核实的"未完成清单"清零：v1.7 发版、端口雷达 IPv6 盲区、设计文档承诺的可调刷新间隔与虚拟列表、v1.6 键盘导航遗留。

**Spec reference:** `docs/superpowers/specs/2026-07-29-codemgr-design.md` §3.2（刷新间隔、虚拟列表）；`docs/superpowers/specs/2026-07-29-codemgr-roadmap-proposals.md`

**问题清单来源（均已核实）：**
1. v1.7 已合入 main 但未发版（AGENTS.md §8 标"未发版"）
2. **IPv6 缺失**：`codemgr-native/src/net_collector.cpp:194` 注释"暂不做 IPv6"，全部调用写死 `AF_INET`（:135/:141/:164/:169）——Vite/Node 默认绑 `::` 的 dev server 在端口雷达完全不可见
3. **虚拟列表未做**：设计文档 §3.2 承诺 >100 进程启用虚拟滚动（支持 2000+），`app/src/components/ProcessTable.tsx`（479 行）全量渲染 `<tr>`
4. **刷新间隔硬编码**：设计文档 §3.2 承诺 1s/2s/5s/暂停可选，`app/src/hooks/usePerf.ts:6`、`useProcessPanel.ts:6`、`usePortRadar.ts:6` 均写死 `POLL_MS`
5. **v1.6 键盘导航遗留**（CHANGELOG 自认）：ContextMenu 菜单项无方向键、LabelRuleEditor 无焦点陷阱、ProcessTable 键盘逻辑无单独单测

**明确不做：** 插件系统、进程快照对比、远程连接（v2.0 远期）；自定义列/排序预设（低价值）；差分推送（当前轮询开销可接受）。

**版本完成判据：**
1. 端口雷达能显示 IPv6 监听（`::` / `::1`）的连接
2. 三个面板的刷新间隔可选（1s/2s/5s/暂停）且重启后保留
3. 进程数 >100 时进程表启用虚拟滚动，键盘导航/树折叠/多选/右键菜单不回归
4. ContextMenu 支持方向键导航；LabelRuleEditor 有焦点陷阱
5. `pnpm test:native && cd app && pnpm vitest run` 全绿；`pnpm bench` netScan p99 无超 3ms 回归

---

## Task 0: 发版 v1.7

- [x] 全量回归（162 PASS，已验证）
- [x] `pnpm build` 全量构建验证（native + 渲染层）
- [x] 打 tag `v1.7`（指向 `102e9d8`）
- [ ] `pnpm dist` 安装包构建留人工（需关第三方杀软实时防护，AGENTS.md §9）

## Task 1: IPv6 连接枚举（native）

**Files:** `codemgr-native/src/net_collector.cpp`、`codemgr-native/tests/net.test.ts`

- [x] TCP/UDP 各增加 `AF_INET6` 的 `GetExtendedTcpTable`/`GetExtendedUdpTable` 调用（同样 OWNER_PID 表类），地址格式化 `inet_ntop(AF_INET6, ...)`；v4/v6 合并进同一返回数组；删除 :194 过时注释
- [x] TDD：net.test.ts 加断言（本机存在 v6 监听则必须出现；否则结构断言）
- [x] `pnpm build:electron` + `pnpm bench`（netScan p99 基线 6.2ms，允许 +3ms）

## Task 2: 可调刷新间隔

**Files:** `app/src/store/processPanelStore.ts`、`portRadarStore.ts`、`perfStore.ts`、三个 hook、三个面板组件、对应测试

- [x] 三个 store 各加 `pollMs: number`（0=暂停）+ `setPollMs`，persist partialize 持久化（TDD）
- [x] 三个 hook 从 store 读间隔，`setInterval` 随值重建；0 时不轮询（保留可见性监听）
- [x] 各面板 header 加间隔选择器（1s/2s/5s/暂停）

## Task 3: 进程表虚拟列表

**Files:** `app/package.json`（加 `@tanstack/react-virtual`）、`app/src/components/ProcessTable.tsx`

- [x] 可见行数 >100 时启用 `useVirtualizer`（树形行固定行高）；<100 保持现状
- [x] **红线**：v1.6 键盘导航（roving tabindex + scrollIntoView + pid 锚定）必须共存——焦点行用 `virtualizer.scrollToIndex`
- [ ] 人工验收：2000 进程模拟滚动流畅；导航/折叠/多选/右键不回归（代码与单测已就位，实机流畅度留人工）
- [ ] 降级方案：冲突无法调和时仅树形表虚拟化并补测试，ProjectGroupView 保持全量（未启用：主方案成功）

## Task 4: a11y 收尾

**Files:** `app/src/components/ContextMenu.tsx`、`LabelRuleEditor.tsx`、新增 `app/tests/processTableKeyboard.test.tsx`

- [x] ContextMenu：↑/↓ 移焦点、Enter 触发、Esc 关闭、Home/End 跳首尾（复用 v1.6 纯导航模型）
- [x] LabelRuleEditor：焦点陷阱（Tab 循环）+ Esc 关闭 + 打开时焦点落首个输入框
- [x] ProcessTable 键盘导航补单测（mock store，复制 PortTable 测试模式）

## Task 5: 收尾

- [x] CHANGELOG `## [v1.8]` 节
- [x] AGENTS.md §8 版本状态更新
- [ ] 全量回归 + bench
- [ ] 打 tag `v1.8`
