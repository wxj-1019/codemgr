# CodeMgr v1.2 实现计划

> **面向 AI 代理的工作者：** 必需子技能：使用 superpowers:subagent-driven-development（推荐）或 superpowers:executing-plans 逐任务实现此计划。步骤使用复选框（`- [ ]`）语法来跟踪进度。

**目标：** 补齐 v1.2 三大功能——端口雷达搜索过滤 + 冲突高亮、按进程查看环境变量、结束进程树（killTree），并顺手修正文档/类型不一致。

**架构：** 端口过滤是纯渲染层改动（lib 纯函数 + store 字段 + UI 输入框），可完整 TDD。环境变量走新 native 采集器 `env_collector.cpp`（OpenProcess + NtQueryInformationProcess 读 PEB 环境块，只读一次、按需触发，不进轮询路径）。killTree 复用现有 `KillByPids` 保护名单逻辑，native 侧按 ppid 链 BFS 收集子孙 PID 后批量结束。两个 native 新函数经 `ipc-types → preload → main → lib/ipc` 四层暴露到渲染层，遵循既有模式。

**技术栈：** C++17 + node-addon-api（native）、Electron IPC、React 18 + Zustand（渲染层）、Vitest（测试）。

---

## 文件结构

**新建：**
- `app/src/lib/portFilter.ts` — 端口连接过滤/监听判定/冲突检测纯函数（从 PortTable 迁入 `isListenLike`）
- `app/tests/portFilter.test.ts` — portFilter 单元测试
- `codemgr-native/src/env_collector.h` / `env_collector.cpp` — 按 PID 读取进程环境变量
- `codemgr-native/tests/env.test.ts` — readProcessEnv 测试

**修改：**
- `codemgr-native/index.ts` — 补 `perfCounters` 声明 + `PerfData` 接口；加 `killTree`/`readProcessEnv` 声明；修正 cwd 注释
- `codemgr-native/src/process_ops.h` / `process_ops.cpp` — 加 `KillTree`/`KillTreeJS`
- `codemgr-native/src/process_collector.h` — 修正 cwd 注释
- `codemgr-native/src/addon.cpp` — 注册 `killTree`、`readProcessEnv`
- `codemgr-native/CMakeLists.txt` — 源文件列表加 `env_collector.cpp`
- `codemgr-native/tests/process.test.ts` — 加 killTree 测试
- `app/electron/ipc-types.ts` — 加 `KILL_TREE`/`FETCH_PROCESS_ENV` 通道 + ExposedApi 方法；修正 cwd 注释
- `app/electron/preload.ts` — 暴露两个新方法
- `app/electron/main.ts` — 两个新 handler
- `app/src/lib/ipc.ts` — 封装两个新方法
- `app/src/store/portRadarStore.ts` — 加 `filter` 字段
- `app/tests/portRadarStore.test.ts` — filter 字段测试
- `app/src/components/PortRadar.tsx` — 搜索框 + 过滤后计数
- `app/src/components/PortTable.tsx` — 改用 portFilter 的 isListenLike；冲突高亮
- `app/src/components/ProcessPanel.tsx` — killTree 确认流程
- `app/src/components/ProcessTable.tsx` — 行内"结束树"按钮
- `app/src/components/ProcessDetailSidebar.tsx` — 环境变量区块 + "结束进程树"按钮
- `README.md` — 修正 clone 地址
- `CHANGELOG.md` — v1.2 条目
- `AGENTS.md` — §8 当前版本状态

---

### 任务 0：文档与类型修正（前置，无行为变更）

**文件：**
- 修改：`codemgr-native/index.ts`
- 修改：`codemgr-native/src/process_collector.h:13`
- 修改：`app/electron/ipc-types.ts:30`
- 修改：`README.md:80`

- [ ] **步骤 1：补 `codemgr-native/index.ts` 的 PerfData 接口与 perfCounters 声明**

在 `CpuUsage` 接口后、`NativeBindings` 前插入：

```ts
// 系统性能计数器快照
export interface PerfData {
  cpu: { totalPercent: number; perCore: number[] };
  memory: { totalBytes: number; availableBytes: number; usedPercent: number };
  disks: Array<{
    name: string;
    totalBytes: number;
    freeBytes: number;
    readBytesPerSec: number;
    writeBytesPerSec: number;
    activePercent: number;
  }>;
  networks: Array<{ name: string; recvBytesPerSec: number; sendBytesPerSec: number }>;
  timestamp: number;
}
```

在 `NativeBindings` 的 `cpuDelta()` 行后加一行：

```ts
  perfCounters(): PerfData;
```

- [ ] **步骤 2：修正 cwd 注释（三处，同一句替换）**

`codemgr-native/index.ts:7`、`app/electron/ipc-types.ts:30`、`codemgr-native/src/process_collector.h:13` 三处的 cwd 注释均改为：

```
cwd: string;             // 当前工作目录（从命令行启发式抽取，非 PEB 直读；见 process_collector.cpp）
```

（C++ 头文件里对应行字段声明为 `std::string cwd;`，只换注释部分。）

- [ ] **步骤 3：修正 README.md:80 的 clone 地址**

把 `git clone .../zenjiro-await.git` 改为 `git clone https://github.com/wxj-1019/codemgr.git`（与实际 remote 一致）。

- [ ] **步骤 4：验证类型检查通过**

运行：`cd app && pnpm typecheck`
预期：无错误退出。

- [ ] **步骤 5：Commit**

```bash
git add codemgr-native/index.ts codemgr-native/src/process_collector.h app/electron/ipc-types.ts README.md
git commit -m "docs: sync type defs and cwd comments with implementation"
```

---

### 任务 1：端口雷达搜索过滤

**文件：**
- 创建：`app/src/lib/portFilter.ts`
- 创建：`app/tests/portFilter.test.ts`
- 修改：`app/src/store/portRadarStore.ts`
- 修改：`app/tests/portRadarStore.test.ts`
- 修改：`app/src/components/PortTable.tsx`
- 修改：`app/src/components/PortRadar.tsx`

- [ ] **步骤 1：编写失败的测试 `app/tests/portFilter.test.ts`**

```ts
import { describe, it, expect } from 'vitest';
import { filterConnections, isListenLike } from '../src/lib/portFilter';
import type { NetConnection } from '../electron/ipc-types';

const conn = (over: Partial<NetConnection> = {}): NetConnection => ({
  protocol: 'tcp',
  localAddr: '0.0.0.0',
  localPort: 3000,
  remoteAddr: '*',
  remotePort: 0,
  state: 'LISTENING',
  pid: 1234,
  processName: 'node',
  ...over,
});

describe('isListenLike', () => {
  it('keeps LISTENING tcp and all udp, drops transient tcp states', () => {
    expect(isListenLike(conn())).toBe(true);
    expect(isListenLike(conn({ protocol: 'udp', state: '' }))).toBe(true);
    expect(isListenLike(conn({ state: 'ESTABLISHED' }))).toBe(false);
    expect(isListenLike(conn({ state: 'TIME_WAIT' }))).toBe(false);
  });
});

describe('filterConnections', () => {
  const list = [
    conn({ localPort: 3000, processName: 'node', pid: 100 }),
    conn({ localPort: 5432, processName: 'postgres', pid: 200 }),
    conn({ localPort: 8080, processName: 'java', pid: 300, localAddr: '127.0.0.1' }),
  ];

  it('empty query returns the original list', () => {
    expect(filterConnections(list, '')).toBe(list);
    expect(filterConnections(list, '   ')).toBe(list);
  });

  it('matches by port number', () => {
    expect(filterConnections(list, '5432').map((c) => c.localPort)).toEqual([5432]);
  });

  it('matches by process name, case-insensitive', () => {
    expect(filterConnections(list, 'NODE').map((c) => c.pid)).toEqual([100]);
  });

  it('matches by pid and local address', () => {
    expect(filterConnections(list, '200').map((c) => c.localPort)).toEqual([5432]);
    expect(filterConnections(list, '127.0.0').map((c) => c.localPort)).toEqual([8080]);
  });

  it('no match returns empty array', () => {
    expect(filterConnections(list, 'zzzz')).toEqual([]);
  });
});
```

- [ ] **步骤 2：运行测试验证失败**

运行：`cd app && pnpm vitest run tests/portFilter.test.ts`
预期：FAIL，报错找不到 `../src/lib/portFilter`。

- [ ] **步骤 3：实现 `app/src/lib/portFilter.ts`**

```ts
import type { NetConnection } from '../../electron/ipc-types';

// 只显示监听/占用端口（LISTENING 的 TCP + 全部 UDP），过滤掉大量瞬态连接。
// （自 PortTable.tsx 迁入，供 PortRadar 计数与冲突检测复用。）
export function isListenLike(c: NetConnection): boolean {
  if (c.protocol === 'udp') return true;
  return c.state === 'LISTENING';
}

// 按端口/进程名/PID/本地地址过滤，大小写不敏感；空查询原样返回（保持引用不变，
// 便于 React memo 比较）。
export function filterConnections(conns: NetConnection[], query: string): NetConnection[] {
  const q = query.trim().toLowerCase();
  if (!q) return conns;
  return conns.filter(
    (c) =>
      String(c.localPort).includes(q) ||
      c.processName.toLowerCase().includes(q) ||
      String(c.pid).includes(q) ||
      c.localAddr.toLowerCase().includes(q),
  );
}
```

- [ ] **步骤 4：运行测试验证通过**

运行：`cd app && pnpm vitest run tests/portFilter.test.ts`
预期：6 个用例全部 PASS。

- [ ] **步骤 5：portRadarStore 加 filter 字段 + 更新测试**

`app/src/store/portRadarStore.ts`：接口中加两行，实现中加对应初始值/动作/reset：

```ts
interface PortRadarState {
  connections: NetConnection[];
  loading: boolean;
  error: string | null;
  selectedPid: number | null;
  filter: string;                                   // 新增
  setConnections: (c: NetConnection[]) => void;
  setLoading: (b: boolean) => void;
  setError: (e: string | null) => void;
  select: (pid: number | null) => void;
  setFilter: (f: string) => void;                   // 新增
  reset: () => void;
}

export const usePortRadarStore = create<PortRadarState>((set) => ({
  connections: [],
  loading: false,
  error: null,
  selectedPid: null,
  filter: '',                                       // 新增
  setConnections: (c) => set({ connections: c, error: null }),
  setLoading: (b) => set({ loading: b }),
  setError: (e) => set({ error: e }),
  select: (pid) => set({ selectedPid: pid }),
  setFilter: (f) => set({ filter: f }),             // 新增
  reset: () => set({ connections: [], loading: false, error: null, selectedPid: null, filter: '' }),
}));
```

`app/tests/portRadarStore.test.ts` 的 `describe('portRadarStore')` 内追加：

```ts
  it('setFilter stores the query and reset clears it', () => {
    usePortRadarStore.getState().setFilter('8080');
    expect(usePortRadarStore.getState().filter).toBe('8080');
    usePortRadarStore.getState().reset();
    expect(usePortRadarStore.getState().filter).toBe('');
  });
```

运行：`cd app && pnpm vitest run tests/portRadarStore.test.ts`
预期：全部 PASS。

- [ ] **步骤 6：PortTable 改用 portFilter 的 isListenLike（消除重复定义）**

`app/src/components/PortTable.tsx`：删除本地 `isListenLike` 函数定义（12–15 行），把 import 行改为：

```ts
import { labelForPort, isDevPort, isDbPort } from '../lib/portLabels';
import { isListenLike } from '../lib/portFilter';
```

- [ ] **步骤 7：PortRadar 加搜索框并接过滤**

`app/src/components/PortRadar.tsx`：

1. import 区加：

```ts
import { filterConnections, isListenLike } from '../lib/portFilter';
```

2. store 解构行改为：

```ts
  const { connections, loading, error, selectedPid, select, filter, setFilter } = usePortRadarStore();
```

3. `listenCount` 计算改为基于过滤后的列表：

```ts
  const visible = filterConnections(connections, filter);
  const listenCount = visible.filter(isListenLike).length;
```

4. header 右侧（`<div>` 标题块之后、`</header>` 之前）加搜索框：

```tsx
        <input
          type="text"
          placeholder="搜索端口/进程/PID/地址…"
          value={filter}
          onChange={(e) => setFilter(e.target.value)}
          className="w-56 rounded border border-base-600 bg-base-800 px-3 py-1 text-sm text-fg-primary placeholder-fg-muted outline-none focus:border-accent/50"
        />
```

5. `<PortTable>` 的 `connections` prop 改为 `visible`。

- [ ] **步骤 8：跑 app 全部测试 + 类型检查**

运行：`cd app && pnpm vitest run && pnpm typecheck`
预期：全部 PASS，类型无错误（PortTable.test.tsx 现有用例不受影响——它传 connections prop，isListenLike 行为未变）。

- [ ] **步骤 9：Commit**

```bash
git add app/src/lib/portFilter.ts app/tests/portFilter.test.ts app/src/store/portRadarStore.ts app/tests/portRadarStore.test.ts app/src/components/PortTable.tsx app/src/components/PortRadar.tsx
git commit -m "feat(app): add port radar search filter"
```

---

### 任务 2：端口冲突高亮

同一端口被两个及以上**不同进程**监听时（SO_REUSEADDR 或 0.0.0.0/127.0.0.1 分别绑定），端口单元格显示红色警告圈，tooltip 说明冲突。

**文件：**
- 修改：`app/src/lib/portFilter.ts`
- 修改：`app/tests/portFilter.test.ts`
- 修改：`app/src/components/PortTable.tsx`

- [ ] **步骤 1：编写失败的测试**

`app/tests/portFilter.test.ts` 顶部 import 行改为：

```ts
import { filterConnections, isListenLike, conflictPorts } from '../src/lib/portFilter';
```

文件末尾追加：

```ts
describe('conflictPorts', () => {
  it('flags a port listened by two different pids', () => {
    const list = [
      conn({ localPort: 8080, pid: 100 }),
      conn({ localPort: 8080, pid: 200 }),
      conn({ localPort: 3000, pid: 100 }),
    ];
    const conflicts = conflictPorts(list);
    expect(conflicts.has(8080)).toBe(true);
    expect(conflicts.has(3000)).toBe(false);
  });

  it('same pid bound to multiple addresses is NOT a conflict', () => {
    const list = [
      conn({ localPort: 8080, pid: 100, localAddr: '0.0.0.0' }),
      conn({ localPort: 8080, pid: 100, localAddr: '::' }),
    ];
    expect(conflictPorts(list).size).toBe(0);
  });

  it('ignores non-listen connections', () => {
    const list = [
      conn({ localPort: 8080, pid: 100, state: 'ESTABLISHED' }),
      conn({ localPort: 8080, pid: 200 }),
    ];
    expect(conflictPorts(list).size).toBe(0);
  });
});
```

- [ ] **步骤 2：运行测试验证失败**

运行：`cd app && pnpm vitest run tests/portFilter.test.ts`
预期：FAIL，`conflictPorts is not a function`。

- [ ] **步骤 3：在 `app/src/lib/portFilter.ts` 末尾追加实现**

> **执行时修订（代码审查发现）：** TCP/UDP 端口号是独立命名空间，聚合键必须带协议维度（`` `${c.protocol}:${c.localPort}` ``，返回 `Set<string>`），PortTable 行内判断同步用 `` conflicts.has(`${c.protocol}:${c.localPort}`) ``。否则 TCP:53 与 UDP:53 不同进程会误报。已实现于 commit e6d6282。

```ts
// 检测端口冲突：同一端口被两个及以上不同 PID 监听（SO_REUSEADDR 场景）。
// 返回冲突端口号集合，供表格高亮。只统计监听态连接；同一进程绑定多个地址不算冲突。
export function conflictPorts(conns: NetConnection[]): Set<number> {
  const owners = new Map<number, Set<number>>();
  for (const c of conns) {
    if (!isListenLike(c)) continue;
    let pids = owners.get(c.localPort);
    if (!pids) {
      pids = new Set<number>();
      owners.set(c.localPort, pids);
    }
    pids.add(c.pid);
  }
  const conflicts = new Set<number>();
  for (const [port, pids] of owners) {
    if (pids.size > 1) conflicts.add(port);
  }
  return conflicts;
}
```

- [ ] **步骤 4：运行测试验证通过**

运行：`cd app && pnpm vitest run tests/portFilter.test.ts`
预期：全部 PASS。

- [ ] **步骤 5：PortTable 冲突高亮**

`app/src/components/PortTable.tsx`：

1. import 改为：

```ts
import { useMemo } from 'react';
import type { NetConnection } from '../../electron/ipc-types';
import { labelForPort, isDevPort, isDbPort } from '../lib/portLabels';
import { isListenLike, conflictPorts } from '../lib/portFilter';
```

2. 组件函数体内 `rows` 计算后加：

```ts
  const conflicts = useMemo(() => conflictPorts(connections), [connections]);
```

3. 端口单元格（`<td className="px-3 py-2 font-mono text-accent">` 那一行）替换为：

```tsx
                <td
                  className={`px-3 py-2 font-mono ${
                    conflicts.has(c.localPort) ? 'text-red-400' : 'text-accent'
                  }`}
                  title={conflicts.has(c.localPort) ? '端口冲突：多个进程监听同一端口' : undefined}
                >
                  {conflicts.has(c.localPort) && <span aria-label="端口冲突">⚠ </span>}
                  {c.localPort}
                </td>
```

- [ ] **步骤 6：跑 app 全部测试**

运行：`cd app && pnpm vitest run && pnpm typecheck`
预期：全部 PASS。

- [ ] **步骤 7：Commit**

```bash
git add app/src/lib/portFilter.ts app/tests/portFilter.test.ts app/src/components/PortTable.tsx
git commit -m "feat(app): highlight conflicting ports in radar"
```

---

### 任务 3：native killTree（结束进程树）

**文件：**
- 修改：`codemgr-native/src/process_ops.h`
- 修改：`codemgr-native/src/process_ops.cpp`
- 修改：`codemgr-native/src/addon.cpp`
- 修改：`codemgr-native/index.ts`
- 修改：`codemgr-native/tests/process.test.ts`

- [ ] **步骤 1：编写失败的测试**

`codemgr-native/tests/process.test.ts` 顶部 import 改为：

```ts
import { describe, it, expect } from 'vitest';
import { spawn } from 'node:child_process';
import native from '../index';
```

文件末尾追加：

```ts
describe('killTree', () => {
  it('kills a spawned parent together with its child', async () => {
    // 父进程：spawn 一个 node 孙进程后挂起
    const parent = spawn(process.execPath, [
      '-e',
      `require('node:child_process').spawn(process.execPath, ['-e', 'setTimeout(()=>{}, 60000)'], { stdio: 'ignore' }); setTimeout(()=>{}, 60000);`,
    ], { stdio: 'ignore' });

    try {
      // 等孙进程起来（最多 5s）
      let childSeen = false;
      for (let i = 0; i < 10 && !childSeen; i++) {
        await new Promise((r) => setTimeout(r, 500));
        childSeen = native.processScan().some((p) => p.ppid === parent.pid);
      }
      expect(childSeen).toBe(true);

      const killed = native.killTree(parent.pid!);
      expect(killed).toBeGreaterThanOrEqual(2); // 父 + 孙

      const after = native.processScan();
      expect(after.some((p) => p.pid === parent.pid)).toBe(false);
    } finally {
      // 兜底清理，避免测试失败时残留
      try { process.kill(parent.pid!); } catch { /* 已退出 */ }
    }
  }, 15000);

  it('never kills protected processes (services.exe root returns 0)', () => {
    const procs = native.processScan();
    const svc = procs.find((p) => p.name.toLowerCase() === 'services.exe');
    expect(svc).toBeDefined();
    // services.exe 在保护名单；其子进程是 svchost.exe 等也在名单内 → 0
    expect(native.killTree(svc!.pid)).toBe(0);
  });

  it('returns 0 for a non-existent pid', () => {
    expect(native.killTree(0x7ffffff0)).toBe(0);
  });
});
```

- [ ] **步骤 2：构建 Node 目标并运行测试验证失败**

运行：`cd codemgr-native && pnpm build && pnpm vitest run tests/process.test.ts`
预期：新增 3 个用例 FAIL，`native.killTree is not a function`。（注意是 `pnpm build` 不是 `build:electron`——vitest 在 Node 下跑。）

- [ ] **步骤 3：实现 killTree**

`codemgr-native/src/process_ops.h` 末尾追加：

```cpp
// Kill a process and all its descendants (BFS over ppid chain).
// Reuses KillByPids: protection list + self-pid guard still apply per pid.
// Returns actual killed count.
size_t KillTree(DWORD rootPid);

Napi::Value KillTreeJS(const Napi::CallbackInfo& info);
```

`codemgr-native/src/process_ops.cpp` 末尾追加：

```cpp
size_t KillTree(DWORD rootPid) {
  std::vector<ProcessInfoRaw> procs;
  std::string err;
  if (!CollectAllProcesses(procs, err)) return 0;

  // ppid -> children pids
  std::unordered_map<DWORD, std::vector<DWORD>> children;
  children.reserve(procs.size());
  for (const auto& p : procs) children[(DWORD)p.ppid].push_back((DWORD)p.pid);

  // 迭代式 DFS 收集整棵子树（含根）。防御自引用：c == pid 不入栈。
  std::vector<DWORD> pids;
  std::vector<DWORD> stack;
  stack.push_back(rootPid);
  while (!stack.empty()) {
    DWORD pid = stack.back();
    stack.pop_back();
    pids.push_back(pid);
    auto it = children.find(pid);
    if (it == children.end()) continue;
    for (DWORD c : it->second) {
      if (c != pid) stack.push_back(c);
    }
  }
  return KillByPids(pids);
}

Napi::Value KillTreeJS(const Napi::CallbackInfo& info) {
  Napi::Env env = info.Env();
  if (info.Length() < 1 || !info[0].IsNumber()) {
    Napi::TypeError::New(env, "expected pid:number").ThrowAsJavaScriptException();
    return env.Null();
  }
  DWORD pid = (DWORD)info[0].As<Napi::Number>().Int32Value();
  return Napi::Number::New(env, (double)KillTree(pid));
}
```

`codemgr-native/src/addon.cpp` 在 `killByPids` 注册行后加：

```cpp
  exports.Set(Napi::String::New(env, "killTree"), Napi::Function::New(env, KillTreeJS));
```

`codemgr-native/index.ts` 的 `NativeBindings` 中 `killByPids` 行后加：

```ts
  killTree(pid: number): number;
```

- [ ] **步骤 4：重新构建并运行测试验证通过**

运行：`cd codemgr-native && pnpm build && pnpm vitest run tests/process.test.ts`
预期：全部 PASS（含原有保护名单用例）。

- [ ] **步骤 5：为 Electron 重编译（AGENTS.md 陷阱 #1）**

运行：`cd codemgr-native && pnpm build:electron`
预期：编译成功，无错误。

- [ ] **步骤 6：Commit**

```bash
git add codemgr-native/src/process_ops.h codemgr-native/src/process_ops.cpp codemgr-native/src/addon.cpp codemgr-native/index.ts codemgr-native/tests/process.test.ts
git commit -m "feat(native): add killTree to terminate process trees"
```

---

### 任务 4：killTree IPC 链路 + 进程面板 UI

**文件：**
- 修改：`app/electron/ipc-types.ts`
- 修改：`app/electron/preload.ts`
- 修改：`app/electron/main.ts`
- 修改：`app/src/lib/ipc.ts`
- 修改：`app/src/components/ProcessPanel.tsx`
- 修改：`app/src/components/ProcessTable.tsx`
- 修改：`app/src/components/ProcessDetailSidebar.tsx`

- [ ] **步骤 1：IPC 通道与类型（ipc-types.ts）**

`IPC` 常量中 `KILL_BY_PIDS` 行后加：

```ts
  KILL_TREE: 'proc:killTree',
```

`ExposedApi` 接口中 `killByPids` 行后加：

```ts
  killTree(pid: number): Promise<number>;
```

- [ ] **步骤 2：preload 暴露**

`app/electron/preload.ts` 的 api 对象中 `killByPids` 行后加：

```ts
  killTree: (pid: number) => ipcRenderer.invoke(IPC.KILL_TREE, pid),
```

- [ ] **步骤 3：main 注册 handler**

`app/electron/main.ts` 的 `KILL_BY_PIDS` handler 之后加：

```ts
ipcMain.handle(IPC.KILL_TREE, async (_evt, pid: number) => {
  try {
    return native.killTree(pid);
  } catch (e) {
    console.error('killTree failed:', e);
    return 0;
  }
});
```

- [ ] **步骤 4：渲染层封装（lib/ipc.ts）**

`app/src/lib/ipc.ts` 的 `killByPids` 方法后加：

```ts
  async killTree(pid: number): Promise<number> {
    return window.codemgr.killTree(pid);
  },
```

- [ ] **步骤 5：ProcessPanel 加 killTree 确认流程**

`app/src/components/ProcessPanel.tsx`：

1. `groupKill` state 声明之后加：

```ts
  // 结束进程树：native 按 ppid 链收集所有子孙后批量结束（保护名单逐 pid 生效）
  const [pendingKillTree, setPendingKillTree] = useState<{
    pid: number;
    name: string;
  } | null>(null);
```

2. `doKillGroup` 函数后加：

```ts
  async function doKillTree() {
    if (!pendingKillTree) return;
    const killed = await ipc.killTree(pendingKillTree.pid);
    setPendingKillTree(null);
    clearSelection();
    alert(`已结束进程树，共 ${killed} 个进程`);
  }
```

3. `<ProcessTable>` 调用改为传入新 prop：

```tsx
            <ProcessTable
              onKillSingle={(pid, name) => setPendingKill({ pid, name })}
              onKillTree={(pid, name) => setPendingKillTree({ pid, name })}
            />
```

4. `<ProcessDetailSidebar>` 调用改为：

```tsx
          <ProcessDetailSidebar
            onKill={(pid, name) => setPendingKill({ pid, name })}
            onKillTree={(pid, name) => setPendingKillTree({ pid, name })}
          />
```

5. 最后一个 `</ConfirmDialog>`（group-kill）之后加：

```tsx
      {/* Kill-tree confirmation — native collects all descendants via ppid chain */}
      <ConfirmDialog
        open={pendingKillTree !== null}
        title="结束进程树"
        message={`确定结束 ${pendingKillTree?.name}（PID ${pendingKillTree?.pid}）及其所有子进程吗？`}
        confirmLabel="结束进程树"
        onConfirm={doKillTree}
        onCancel={() => setPendingKillTree(null)}
      />
```

- [ ] **步骤 6：ProcessTable 行内"结束树"按钮**

`app/src/components/ProcessTable.tsx`：

1. props 接口改为：

```ts
interface ProcessTableProps {
  onKillSingle: (pid: number, name: string) => void;
  onKillTree: (pid: number, name: string) => void;
}
```

2. `ProcessRowProps` 加一行：

```ts
  onKillTree: (pid: number, name: string) => void;
```

3. `ProcessRow` 解构参数加 `onKillTree`；"结束"按钮的 `</button>` 后加（仅对有子进程的行显示）：

```tsx
        {hasChildren && (
          <button
            onClick={(e) => {
              e.stopPropagation();
              onKillTree(proc.pid, proc.name);
            }}
            className="ml-1 rounded bg-orange-600/80 px-1.5 py-0.5 text-[10px] text-white hover:bg-orange-500"
            title="结束该进程及其所有子进程"
          >
            树
          </button>
        )}
```

4. `ProcessTable` 组件签名改为 `export function ProcessTable({ onKillSingle, onKillTree }: ProcessTableProps) {`，`<ProcessRow>` 传参加 `onKillTree={onKillTree}`。

- [ ] **步骤 7：ProcessDetailSidebar 加"结束进程树"按钮**

`app/src/components/ProcessDetailSidebar.tsx`：

1. 组件签名改为：

```ts
export function ProcessDetailSidebar({
  onKill,
  onKillTree,
}: {
  onKill: (pid: number, name: string) => void;
  onKillTree: (pid: number, name: string) => void;
}) {
```

2. 底部"结束进程"按钮的 `</button>` 后加：

```tsx
        <button
          onClick={() => onKillTree(proc.pid, proc.name)}
          className="mt-2 w-full rounded bg-orange-600/80 px-3 py-1.5 text-sm text-white hover:bg-orange-500"
        >
          结束进程树
        </button>
```

- [ ] **步骤 8：类型检查 + app 测试 + 人工验收**

运行：`cd app && pnpm typecheck && pnpm vitest run`
预期：PASS（ProcessTable/DetailSidebar 的 props 变化不影响现有测试用例，它们不直接渲染这两个组件）。

人工验收（`pnpm dev`）：展开一个有子进程的 dev server（如 pnpm → node），点"树"按钮，确认父+子全部消失。

- [ ] **步骤 9：Commit**

```bash
git add app/electron/ipc-types.ts app/electron/preload.ts app/electron/main.ts app/src/lib/ipc.ts app/src/components/ProcessPanel.tsx app/src/components/ProcessTable.tsx app/src/components/ProcessDetailSidebar.tsx
git commit -m "feat(app): kill process tree from table row and detail sidebar"
```

---

### 任务 5：native readProcessEnv（按进程读取环境变量）

实现方式：OpenProcess（QUERY_LIMITED_INFORMATION + VM_READ）→ NtQueryInformationProcess(ProcessBasicInformation) 拿 PEB 地址 → ReadProcessMemory 读 PEB→ProcessParameters→Environment 指针 → 分块读环境块（UTF-16、双 null 结尾）→ 解析为 JS 对象。**只读、按需触发**，不进轮询路径，不影响 p99 基线。仅支持 x64（项目既定平台）。

**文件：**
- 创建：`codemgr-native/src/env_collector.h`
- 创建：`codemgr-native/src/env_collector.cpp`
- 创建：`codemgr-native/tests/env.test.ts`
- 修改：`codemgr-native/CMakeLists.txt`
- 修改：`codemgr-native/src/addon.cpp`
- 修改：`codemgr-native/index.ts`

- [ ] **步骤 1：编写失败的测试 `codemgr-native/tests/env.test.ts`**

```ts
import { describe, it, expect } from 'vitest';
import native from '../index';

describe('readProcessEnv', () => {
  it('reads env of the current process (PATH must exist)', () => {
    const env = native.readProcessEnv(process.pid);
    expect(typeof env).toBe('object');
    const keys = Object.keys(env);
    expect(keys.length).toBeGreaterThan(10);
    expect(keys.some((k) => k.toUpperCase() === 'PATH')).toBe(true);
  });

  it('skips hidden entries like =C:= (drive-letter vars)', () => {
    const env = native.readProcessEnv(process.pid);
    for (const k of Object.keys(env)) {
      expect(k.startsWith('=')).toBe(false);
    }
  });

  it('throws for a non-existent pid', () => {
    expect(() => native.readProcessEnv(0x7ffffff0)).toThrow();
  });
});
```

- [ ] **步骤 2：运行测试验证失败**

运行：`cd codemgr-native && pnpm vitest run tests/env.test.ts`
预期：FAIL，`native.readProcessEnv is not a function`。

- [ ] **步骤 3：实现 env_collector**

创建 `codemgr-native/src/env_collector.h`：

```cpp
#pragma once
#include <napi.h>

// 读取目标进程环境变量。成功返回 JS 对象 {KEY: value}；
// 失败（权限不足 / 进程已退出 / 读取失败）抛 JS 异常，由调用方降级处理。
// 仅支持 x64 Windows（PEB 偏移为 64 位布局）。
Napi::Value ReadProcessEnv(const Napi::CallbackInfo& info);
```

创建 `codemgr-native/src/env_collector.cpp`：

```cpp
#include "env_collector.h"
#include <windows.h>
#include <string>

// --- NT 类型（遵循项目惯例：自定义结构避免 winternl.h 冲突，见 AGENTS.md 陷阱 #3） ---
typedef NTSTATUS(NTAPI* NtQueryInformationProcess_t)(
  HANDLE ProcessHandle, ULONG ProcessInformationClass,
  PVOID ProcessInformation, ULONG ProcessInformationLength, PULONG ReturnLength);

struct PBI_LOCAL {
  PVOID Reserved1;
  PVOID PebBaseAddress;
  PVOID Reserved2[2];
  ULONG_PTR UniqueProcessId;
  PVOID Reserved3;
};

// x64 PEB 布局偏移：PEB+0x20 = ProcessParameters；RTL_USER_PROCESS_PARAMETERS+0x80 = Environment
static const SIZE_T OFFSET_PEB_PROCESS_PARAMETERS = 0x20;
static const SIZE_T OFFSET_RUPP_ENVIRONMENT = 0x80;

// 从远程进程读一个指针；失败返回 false
static bool ReadRemotePtr(HANDLE h, const void* addr, ULONG_PTR& out) {
  SIZE_T read = 0;
  return ReadProcessMemory(h, addr, &out, sizeof(out), &read) && read == sizeof(out) && out != 0;
}

Napi::Value ReadProcessEnv(const Napi::CallbackInfo& info) {
  Napi::Env env = info.Env();
  if (info.Length() < 1 || !info[0].IsNumber()) {
    Napi::TypeError::New(env, "expected pid:number").ThrowAsJavaScriptException();
    return env.Null();
  }
  DWORD pid = (DWORD)info[0].As<Napi::Number>().Int32Value();

  HANDLE h = OpenProcess(PROCESS_QUERY_LIMITED_INFORMATION | PROCESS_VM_READ, FALSE, pid);
  if (!h) {
    Napi::Error::New(env, "open process failed (access denied or exited)")
      .ThrowAsJavaScriptException();
    return env.Null();
  }

  auto ntqip = (NtQueryInformationProcess_t)GetProcAddress(
    GetModuleHandleW(L"ntdll.dll"), "NtQueryInformationProcess");
  if (!ntqip) {
    CloseHandle(h);
    Napi::Error::New(env, "NtQueryInformationProcess unavailable").ThrowAsJavaScriptException();
    return env.Null();
  }

  PBI_LOCAL pbi{};
  NTSTATUS st = ntqip(h, 0 /*ProcessBasicInformation*/, &pbi, sizeof(pbi), nullptr);
  if (st != 0 || !pbi.PebBaseAddress) {
    CloseHandle(h);
    Napi::Error::New(env, "query PEB failed").ThrowAsJavaScriptException();
    return env.Null();
  }

  ULONG_PTR paramsAddr = 0, envAddr = 0;
  if (!ReadRemotePtr(h, (const BYTE*)pbi.PebBaseAddress + OFFSET_PEB_PROCESS_PARAMETERS, paramsAddr) ||
      !ReadRemotePtr(h, (const BYTE*)paramsAddr + OFFSET_RUPP_ENVIRONMENT, envAddr)) {
    CloseHandle(h);
    Napi::Error::New(env, "read environment pointer failed").ThrowAsJavaScriptException();
    return env.Null();
  }

  // 环境块 = UTF-16 "K=V\0" 序列，双 \0 结尾。分块读直到拿到结尾；上限 64 块（512KB）防坏数据。
  std::wstring block;
  wchar_t chunk[4096];
  for (int i = 0; i < 64; i++) {
    SIZE_T read = 0;
    if (!ReadProcessMemory(h, (const BYTE*)envAddr + (SIZE_T)i * sizeof(chunk),
                           chunk, sizeof(chunk), &read) || read == 0) break;
    block.append(chunk, read / sizeof(wchar_t));
    if (block.find(L"\0\0") != std::wstring::npos) break;
  }
  CloseHandle(h);

  if (block.empty()) {
    Napi::Error::New(env, "environment block empty or unreadable").ThrowAsJavaScriptException();
    return env.Null();
  }

  // 解析为 JS 对象。跳过以 '=' 开头的隐藏变量（如 =C:=、=ExitCode）。
  // Napi::String::New 支持 char16_t* 重载，Windows wchar_t 即 16 位，直接强转。
  Napi::Object result = Napi::Object::New(env);
  size_t pos = 0;
  while (pos < block.size()) {
    size_t end = block.find(L'\0', pos);
    if (end == std::wstring::npos || end == pos) break;
    const wchar_t* entry = block.data() + pos;
    size_t len = end - pos;
    pos = end + 1;
    size_t eq = block.find(L'=', pos - len);   // 在 entry 内找 '='
    if (eq == std::wstring::npos || eq >= pos - 1 || eq == pos - len) continue; // 无 '=' 或首字符 '='
    size_t keyLen = eq - (pos - len);
    size_t valLen = len - keyLen - 1;
    result.Set(
      Napi::String::New(env, (const char16_t*)entry, keyLen),
      Napi::String::New(env, (const char16_t*)(entry + keyLen + 1), valLen));
  }
  return result;
}
```

`codemgr-native/CMakeLists.txt` 的 `add_library` 源列表中 `src/perf_collector.cpp` 行后加：

```cmake
  src/env_collector.cpp
```

`codemgr-native/src/addon.cpp`：include 区加 `#include "env_collector.h"`，`perfCounters` 注册行后加：

```cpp
  exports.Set(Napi::String::New(env, "readProcessEnv"), Napi::Function::New(env, ReadProcessEnv));
```

`codemgr-native/index.ts` 的 `NativeBindings` 中 `killTree` 行后加：

```ts
  readProcessEnv(pid: number): Record<string, string>;
```

- [ ] **步骤 4：构建并运行测试验证通过**

运行：`cd codemgr-native && pnpm build && pnpm vitest run`
预期：全部 PASS（env 3 例 + process/net 既有用例）。

- [ ] **步骤 5：为 Electron 重编译**

运行：`cd codemgr-native && pnpm build:electron`
预期：编译成功。

- [ ] **步骤 6：Commit**

```bash
git add codemgr-native/src/env_collector.h codemgr-native/src/env_collector.cpp codemgr-native/tests/env.test.ts codemgr-native/CMakeLists.txt codemgr-native/src/addon.cpp codemgr-native/index.ts
git commit -m "feat(native): read process environment variables via PEB"
```

---

### 任务 6：环境变量 IPC 链路 + 详情侧栏 UI

侧栏加"环境变量"区块：**默认不加载**（避免每 2s 轮询打满 ReadProcessMemory），点按钮按需读取；切换选中进程时清空。

**文件：**
- 修改：`app/electron/ipc-types.ts`
- 修改：`app/electron/preload.ts`
- 修改：`app/electron/main.ts`
- 修改：`app/src/lib/ipc.ts`
- 修改：`app/src/components/ProcessDetailSidebar.tsx`

- [ ] **步骤 1：IPC 通道与类型（ipc-types.ts）**

`IPC` 常量中 `KILL_TREE` 行后加：

```ts
  FETCH_PROCESS_ENV: 'proc:fetchEnv',
```

`ExposedApi` 接口中 `killTree` 行后加：

```ts
  fetchProcessEnv(pid: number): Promise<Record<string, string> | null>;
```

（`null` = 读取失败：权限不足或进程已退出，UI 据此降级提示。）

- [ ] **步骤 2：preload 暴露**

`app/electron/preload.ts` 的 api 对象中 `killTree` 行后加：

```ts
  fetchProcessEnv: (pid: number) => ipcRenderer.invoke(IPC.FETCH_PROCESS_ENV, pid),
```

- [ ] **步骤 3：main 注册 handler**

`app/electron/main.ts` 的 `KILL_TREE` handler 之后加：

```ts
ipcMain.handle(IPC.FETCH_PROCESS_ENV, async (_evt, pid: number) => {
  try {
    return native.readProcessEnv(pid);
  } catch (e) {
    console.error('readProcessEnv failed:', e);
    return null;  // 权限不足 / 进程已退出：渲染层显示降级提示
  }
});
```

- [ ] **步骤 4：渲染层封装（lib/ipc.ts）**

`app/src/lib/ipc.ts` 的 `killTree` 方法后加：

```ts
  async fetchProcessEnv(pid: number): Promise<Record<string, string> | null> {
    return window.codemgr.fetchProcessEnv(pid);
  },
```

- [ ] **步骤 5：ProcessDetailSidebar 环境变量区块**

`app/src/components/ProcessDetailSidebar.tsx`：

1. import 区加：

```ts
import { useEffect, useState } from 'react';
import { ipc } from '../lib/ipc';
```

2. 组件内 `const pid = [...selectedPids][0];` 之后加：

```ts
  // 环境变量：按需加载，切换选中进程时重置（不做轮询，避免高频 ReadProcessMemory）
  const [envVars, setEnvVars] = useState<Record<string, string> | null>(null);
  const [envState, setEnvState] = useState<'idle' | 'loading' | 'error' | 'done'>('idle');
  useEffect(() => {
    setEnvVars(null);
    setEnvState('idle');
  }, [pid]);

  async function loadEnv() {
    setEnvState('loading');
    const result = await ipc.fetchProcessEnv(pid);
    if (result === null) {
      setEnvState('error');
    } else {
      setEnvVars(result);
      setEnvState('done');
    }
  }
```

3. `<dl>` 的最后一个 `<Row>`（句柄数）之后加：

```tsx
          <div>
            <dt className="text-fg-muted">环境变量</dt>
            <dd className="mt-0.5">
              {envState === 'idle' && (
                <button onClick={loadEnv} className="text-accent hover:underline">
                  加载环境变量
                </button>
              )}
              {envState === 'loading' && <span className="text-fg-muted">读取中…</span>}
              {envState === 'error' && (
                <span className="text-fg-muted">读取失败：权限不足或进程已退出</span>
              )}
              {envState === 'done' && envVars && (
                <div className="max-h-48 overflow-auto rounded border border-base-700 bg-base-900 p-2 font-mono text-[11px]">
                  {Object.keys(envVars).sort().map((k) => (
                    <div key={k} className="break-all">
                      <span className="text-accent">{k}</span>
                      <span className="text-fg-muted">=</span>
                      <span className="text-fg-secondary">{envVars[k]}</span>
                    </div>
                  ))}
                </div>
              )}
            </dd>
          </div>
```

（`bg-base-900` 若不在 tailwind 配置的颜色阶内，改用 `bg-base-800`——以 `app/tailwind.config.ts` 实际定义为准。）

- [ ] **步骤 6：类型检查 + app 测试 + 人工验收**

运行：`cd app && pnpm typecheck && pnpm vitest run`
预期：PASS。

人工验收（`pnpm dev`）：选中一个 node.exe 进程 → 侧栏点"加载环境变量" → 看到 PATH/NODE_ENV 等条目；选中受保护系统进程 → 显示降级提示。

- [ ] **步骤 7：Commit**

```bash
git add app/electron/ipc-types.ts app/electron/preload.ts app/electron/main.ts app/src/lib/ipc.ts app/src/components/ProcessDetailSidebar.tsx
git commit -m "feat(app): view process environment variables in detail sidebar"
```

---

### 任务 7：收尾（文档 + 全量验证 + bench）

**文件：**
- 修改：`CHANGELOG.md`
- 修改：`AGENTS.md`

- [ ] **步骤 1：CHANGELOG.md 加 v1.2 条目**

在文件顶部（现有最新版本条目之前）加：

```markdown
## [v1.2] - 2026-07-29

### Added
- 端口雷达：搜索过滤（端口/进程名/PID/地址）
- 端口雷达：端口冲突高亮（多进程监听同一端口时 ⚠ 标红）
- 进程：结束进程树（killTree），表格行与详情侧栏均可触发
- 进程详情侧栏：按需查看进程环境变量

### Fixed
- codemgr-native/index.ts 补齐 perfCounters 类型声明
- cwd 字段注释与实际实现（命令行启发式抽取）对齐
- README clone 地址修正为当前仓库
```

- [ ] **步骤 2：AGENTS.md §8 加 v1.2 行**

在 `## 8. 当前版本状态` 的 v1.1 行之前加：

```markdown
- **v1.2**（tag `v1.2`）：端口雷达搜索过滤 + 冲突高亮 + 结束进程树 + 进程环境变量查看。
```

- [ ] **步骤 3：全量测试**

运行：`pnpm test:native && cd app && pnpm vitest run && pnpm typecheck`
预期：native 全部 PASS（新增 killTree 3 例 + env 3 例）、app 全部 PASS、typecheck 无错误。

- [ ] **步骤 4：性能基准（native 采集层有改动，按 AGENTS.md 必须跑）**

运行：`pnpm bench`
预期：processScan p99 ≤ 20ms、netScan p99 ≤ 5ms、60s 无泄漏——与 v1.1 基线（17.7ms / 3ms）一致。killTree/readProcessEnv 不在轮询路径，不应影响基线；若出现回归，排查是否误改了 processScan 路径。

- [ ] **步骤 5：Commit**

```bash
git add CHANGELOG.md AGENTS.md
git commit -m "docs: v1.2 changelog and version status"
```

---

## 自检结论

- **规格覆盖度：** 分析报告的 v1.2 三大功能各有对应任务：端口搜索过滤（任务 1）+ 冲突高亮（任务 2）、killTree（任务 3+4）、环境变量（任务 5+6）；文档/类型不一致修正为任务 0。虚拟列表、可调刷新间隔、安装包明确**不在** v1.2 范围（留待 v1.3）。
- **类型一致性：** `killTree(pid: number): Promise<number>`（ExposedApi）↔ `killTree(pid: number): number`（NativeBindings）↔ UI 调用一致；`fetchProcessEnv` 返回 `Record<string, string> | null` 三处一致（ExposedApi / ipc.ts / sidebar 的 null 降级判断）。
- **已知风险：** 任务 5 的 PEB 偏移是 x64 硬编码（项目仅面向 x64 Windows，spec 既定）；32 位进程（WoW64）的 PEB 布局不同，v1.2 读取 32 位进程环境变量会返回错误提示而非崩溃——可接受，留作后续改进。
